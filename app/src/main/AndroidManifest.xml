<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ─── Permissions ─── -->
    <!-- Dial *99# programmatically via TelephonyManager -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!-- Detect call interruptions during USSD session -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- App-lock biometric authentication -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Optional: parse bank confirmation SMS -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <!-- Optional: import device contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- GSM voice capability required for USSD -->
    <uses-feature
        android:name="android.hardware.telephony.gsm"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="true" />

    <application
        android:name=".PayOffApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PayOff"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.PayOff.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
